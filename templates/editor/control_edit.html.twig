{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}{{ 'Editor'|trans ~ ' - ' ~ 'Control'|trans }}{% endblock %}
{% block page_actions %}
    <a class="btn btn-secondary" href="{{ ea_url().setRoute('editor_control_index') }}">
        {{ 'Return to index'|trans }}
    </a>
{% endblock %}
{% block main %}
    <h1>{{ 'Create Control'|trans }}</h1>

    <div class="container mt-2">
        {{ errors }}
    </div>
    
    <div class="container">
        {{ form(form) }}
    </div>
{% endblock %}